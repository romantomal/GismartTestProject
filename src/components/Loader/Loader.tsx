import React from 'react';
import {LoaderContainer, LoaderData} from "components/Loader/style";

const Loader = () => {
    return (
        <LoaderContainer>
            <LoaderData/>
        </LoaderContainer>
    )
};

export default Loader;
